import 'package:flutter/material.dart';

class ColorRepository {
  static Color blue = const HSLColor.fromAHSL(1.0, 246, 0.8, 0.6).toColor();
  static Color lightRedWork = const HSLColor.fromAHSL(1.0, 15, 1.0, 0.7).toColor();
  static Color lightRedStudy = const HSLColor.fromAHSL(1.0, 348, 1.0, 0.68).toColor();
  static Color softBluePlay = const HSLColor.fromAHSL(1.0, 195, 0.74, 0.62).toColor();
  static Color limeGreen = const HSLColor.fromAHSL(1.0, 145, 0.58, 0.55).toColor();
  static Color violet = const HSLColor.fromAHSL(1.0, 264, 0.64, 0.52).toColor();
  static Color softOrange = const HSLColor.fromAHSL(1.0, 43, 0.84, 0.65).toColor();
  static Color doubleDarkBlue = const HSLColor.fromAHSL(1.0, 226, 0.43, 0.1).toColor();
  static Color darkBlue = const HSLColor.fromAHSL(1.0, 235, 0.46, 0.2).toColor();
  static Color desaturatedBlue = const HSLColor.fromAHSL(1.0, 235, 0.45, 0.61).toColor();
  static Color paleBlue = const HSLColor.fromAHSL(1.0, 236, 1.0, 0.87).toColor();


}
